// Pedir al usuario que ingrese el número//
//let input = parseInt(prompt("Digite el número entero para crear la tabla de multiplicar: "));

// Para introducir los encabezados de la tabla
//let resultado = "<tr><th>Multiplicación</th><th>Resultado</th></tr>";

// Ciclo o bucle para que se repita una acción. Multiplicar 12 veces el número dado por el usuario.
//for (let i = 1; i <= 12; i++) {
    resultado += `<tr><td>${input} X ${i} =</td><td>${input * i}</td></tr>`;


// Mostrar en pantalla el resultado
//document.getElementById('tabla').innerHTML = resultado;